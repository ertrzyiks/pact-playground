{
  "name": "pact-playground",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "consumer-pact": "yarn workspace consumer test && yarn workspace consumer publish:pact",
    "consumer-pact:production": "yarn workspace consumer test && env TRAVIS_COMMIT=456 TRAVIS_BRANCH=production yarn workspace consumer publish:pact",
    "verify": "yarn workspace provider verify:pact",
    "select": "yarn workspace debug select"
  }
}
